# ⚜️ Империум Линк — мессенджер Империи Гиперион

Официальное средство связи цифрового государства **Империя Гиперион**.
Киберпанк + имперский неоклассицизм: чёрно-синяя цитадель, золотые акценты,
Playfair Display для указов, Inter для посланий.

> Репозиторий: https://github.com/Hyperion612/Messenger_Imperium-Link

## ✦ Возможности

- **Врата цитадели** — вход по Гиперион-ID (`HIT-XXXXX`), двухфакторная аутентификация,
  восстановление пароля через почту, сессии с истечением срока (24 ч).
- **Чаты** — личные послания (E2E-шифрование), группы до 1000 участников, каналы
  с неограниченной подпиской, автоматические комнаты провинций (Аврора, Бореаль,
  Кристаллис, Элизиум), закрепы, архив, беззвучный режим.
- **Сообщения** — текст с эмодзи, фото, документы, голосовые (запись + волновой плеер),
  ответы, пересылка, редактирование (окно 5 минут), удаление с пометкой, галочки ✓ / ✓✓.
- **Поиск** — по сообщениям, чатам и гражданам, фильтры по типу и дате.
- **Реальное время** — симуляция WebSocket-канала: присутствие, «печатает…»,
  входящие послания, push-уведомления (настраиваются), упоминания @имя, звуки.
- **Ранги Империи** — НОВИЧОК → ГРАЖДАНИН → СТРАЖ → ОФИЦЕР → ГЕНЕРАЛ → СЕНАТОР,
  привилегии gated по рангу (группы — с ГРАЖДАНИНА, каналы — с ОФИЦЕРА).
- **Имперские функции** — канал Императора (только чтение), системные законы,
  ИИ-ОКО (модерация запрещённых слов), Уровень Света (Искра → Вечное Пламя),
  награды за активность, переводы HYPER через Казначейство, покупки на Рынке.
- **PWA** — установка на устройство, оффлайн-кэш, manifest + service worker.

## 🚀 Запуск

```bash
npm install
npm run dev        # http://localhost:3000
npm run build      # production-сборка в dist/
```

## 🔑 Данные входа

| Поле | Значение |
|---|---|
| Гиперион-ID | `HIT-77777` |
| Пароль | `hyperion` |
| 2FA-код | отображается в push-уведомлении на экране (демо) |

## 📁 Структура

```
src/
  App.tsx            каркас: рейл Империи, слои фона, мобильная навигация
  main.tsx           вход + регистрация service worker (PWA)
  types.ts           доменная модель: Citizen, Chat, Message, Rank…
  store.tsx          состояние, действия, WebSocket-симуляция, звук, localStorage
  icons.tsx          40+ кастомных SVG, Имперская печать, лавровые ветви
  index.css          тема Империи, анимации, glass-слои
  data/seed.ts       граждане, чаты, история, указы, рынок, реплики ботов
  components/
    Login.tsx        врата цитадели: вход, 2FA, восстановление пароля
    Sidebar.tsx      список чатов, вкладки, фильтры, контекстное меню
    ChatView.tsx     сообщения, композер, голосовые, эмодзи, превью файлов
    Overlays.tsx     профиль/казначейство/рынок/поиск/жалобы/новые чаты
.github/workflows/pages.yml   автодеплой на GitHub Pages
public/              manifest.webmanifest, sw.js
```

## 🌐 Публикация в репозиторий

В репозитории уже лежит `README.md`, поэтому истории нужно соединить:

```bash
cd Messenger_Imperium-Link            # папка с файлами проекта
git init
git add .
git commit -m "feat: Империум Линк — мессенджер Империи Гиперион"
git branch -M main
git remote add origin https://github.com/Hyperion612/Messenger_Imperium-Link.git
git pull origin main --allow-unrelated-histories --no-edit
git push -u origin main
```

> Если файл `README.md` конфликтует при слиянии — оставьте новую версию:
> `git checkout --theirs README.md && git add README.md && git commit --no-edit`

## 🏛 GitHub Pages (автодеплой)

Workflow `Build & Deploy to GitHub Pages` запускается на каждый push в `main`,
собирает проект с относительными путями (`vite build --base=./`) и
**сам публикует готовую сборку в ветку `gh-pages`**.

Один раз настроить:

1. Сделайте push (workflow запустится автоматически — вкладка **Actions**).
2. Дождитесь зелёной галочки (~1–2 мин). В репозитории появится ветка `gh-pages`.
3. **Settings → Pages → Build and deployment → Source** → **«Deploy from a branch»**.
4. **Branch** → выбрать **`gh-pages`**, папка `/ (root)` → **Save**.
5. Открыть сайт, жёсткое обновление `Ctrl + Shift + R`.

Готово: `https://hyperion612.github.io/Messenger_Imperium-Link/`.
Каждый следующий push в `main` обновляет ветку `gh-pages` и сайт автоматически.

> Если job `deploy` завис в статусе *Waiting* — это окружение `github-pages`
> ждёт подтверждения (Settings → Environments → github-pages → Required reviewers).
> Можно одобрить, а можно игнорировать: сайт уже опубликован из ветки `gh-pages`.

## ⚠️ Если на сайте белый экран или заставка «Связь с цитаделью не установлена»

Это значит, что Pages отдаёт **исходники репозитория**, а не собранную сборку.
Причина — в Pages выбрана ветка `main` (или корень репозитория). Исправление:

1. Вкладка **Actions** → убедиться, что workflow
   **«Build & Deploy to GitHub Pages»** отработал с зелёной галочкой
   (иначе — запустить вручную: **Run workflow** → ветка `main`).
2. **Settings → Pages → Source: «Deploy from a branch» → Branch: `gh-pages`** → Save.
3. Жёсткое обновление `Ctrl + Shift + R`. Если показывает старое — DevTools →
   Application → Service Workers → Unregister.

Если скрипты не загрузились, вместо белого экрана появится загрузочная
заставка Империи с подсказкой.

## 📜 Лицензия

MIT © 2026 Hyperion612 — Империя Гиперион
